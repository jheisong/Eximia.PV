/*import React, { Component } from 'react';
import { bindActionCreators } from 'redux';
import * as receivablesActions from '../actions/receivables';
import { connect } from 'react-redux';


class receivableList extends Component {
    render() {
        return (
            <React.Fragment>
                <div>TEste</div>
            </React.Fragment>       
        )
    }
}

/*const mapDispatchToProps = dispatch => bindActionCreators(receivablesActions, dispatch);

export default connect(null, mapDispatchToProps)(receivableList); 

export default receivableList;*/